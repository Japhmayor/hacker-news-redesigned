import { stripUnit } from 'polished';

import { spacing } from './spacing';


export const containerWidth = 980;
export const containerGutterSmall = stripUnit(spacing(6));
export const containerGutterLarge = stripUnit(spacing(10));
