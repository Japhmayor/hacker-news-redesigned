import styled from 'styled-components';
import { spacing } from '../../styles-old/settings/spacing';

const PollTotal = styled.div`
  text-align: right;
  margin-top: ${spacing(3)};
`;

export default PollTotal;
